# Version metadata used by configure.
#
# Upstream ncurses used this path as a large distribution/release Makefile,
# and configure mined these three values from it.  The release targets were
# deliberately pruned; keep only the build metadata until configure itself is
# simplified enough to own the version directly.
#
# See source-first-layout/history for the former distribution machinery.

NCURSES_MAJOR = 6
NCURSES_MINOR = 4
NCURSES_PATCH = 20230311
